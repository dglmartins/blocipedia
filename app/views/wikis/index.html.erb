<h1>All Wikis</h1>
<% if user_signed_in? %>
  <p>Here you can see all public Wikis, and all your private Wikis</p>
<% else %>
  <p>All public Wikis.</p>
<% end %>
 <% @wikis.each do |wiki| %>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%= link_to wiki.title, wiki %>
       </h4>
     </div>
   </div>
 <% end %>
  <%= link_to "New Wiki", new_wiki_path, class: 'btn btn-success' %>
